import styled from 'styled-components';

const QueueContainer = styled.div`
	display: flex;
	flex-wrap: wrap;
	width: 600px;
`;

const TitleContainer = styled.div`
	display: flex;
	flex-direction: column;
`;

export { QueueContainer, TitleContainer };
